{
    "name": "trading-filter",
    "version": "2.0.0",
    "description": "Simple stock filter viewer with VN30 labeling",
    "main": "server.js",
    "scripts": {
        "start": "node server.js",
        "dev": "node server.js",
        "test": "node --test tests/*.test.js",
        "git:prune-branches": "git fetch --prune && git for-each-ref --format='%(refname:short) %(upstream:track)' refs/heads | awk '$NF == \"[gone]\" { print $1 }' | while IFS= read -r branch; do git branch -d \"$branch\"; done"
    },
    "keywords": [
        "stock",
        "trading",
        "vn30",
        "tradingview"
    ],
    "author": "",
    "license": "MIT",
    "dependencies": {
        "express": "^4.18.2",
        "cors": "^2.8.5"
    }
}
