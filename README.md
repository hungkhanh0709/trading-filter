# 📊 Stock Filter - VN30 Tracker

Web app đơn giản hiển thị danh sách mã cổ phiếu đã lọc, với đánh dấu VN30 và link TradingView.

## ✨ Tính năng

- ✅ Hiển thị danh sách mã cổ phiếu theo ngày
- ✅ Đánh dấu VN30 bằng icon ⭐ bên cạnh symbol
- ✅ Link trực tiếp tới TradingView chart
- ✅ Chọn ngày xem data lịch sử
- ✅ UI Material Design hiện đại (Vuetify 3)
- ✅ Tìm kiếm nhanh
- ✅ Thống kê: tổng số mã, số mã VN30
- ✅ Xử lý trùng lặp, tổ chức data theo ngày
- ✅ Tách biệt file raw input và data storage

## 🚀 Sử dụng

### Khởi động

```bash
npm start
```

Mở trình duyệt: **http://localhost:3000**

## 📝 Workflow hàng ngày

### 1. Input data thô (mỗi sáng)

Mở file `data/raw.json` và paste danh sách mã:

```json
{
  "symbols": "ACB,BID,VTP,SAB,HPG,MBB,VCB"
}
```

### 2. Process data

**Tự động khi start server:**
```bash
npm start
```

**Hoặc manual bằng nút:**
- Click icon 📥 (file-import) trên app bar
- Hoặc reload page

### 3. Kết quả

- `raw.json` → đọc symbols
- Ngày hiện tại → 20260119
- `data.json` → append/merge với ngày hiện tại
- Deduplication tự động
- UI → hiển thị data mới

## 📁 Cấu trúc Files

```
trading-filter/
├── server.js              # Express backend
├── package.json           
├── data/
│   ├── vn30.json         # Danh sách VN30 (reference)
│   ├── raw.json          # INPUT: Data thô hàng ngày (SAFE TO EDIT)
│   └── data.json         # STORAGE: Data đã xử lý (AUTO-GENERATED)
└── public/
    └── index.html         # Vue 3 + Vuetify 3 UI
```

### File Purposes

**`raw.json`** - File input đơn giản:
- ✅ SAFE to edit manually
- ✅ Simple format: chỉ có symbols
- ✅ Paste data mới mỗi ngày
- ❌ Không lo làm hỏng data cũ

**`data.json`** - Database có cấu trúc:
- ✅ Auto-generated by server
- ✅ Organized by date
- ✅ Deduplicated
- ❌ KHÔNG edit manually

## 🔄 Data Processing Flow

```
[raw.json] 
    ↓ (edit mỗi ngày)
"ACB,BID,VTP"
    ↓
[Server reads + gets current date]
    ↓
Date: 20260119
    ↓
[Check data.json for 20260119]
    ↓
Exists? → Merge symbols + deduplicate
Not exists? → Add new entry
    ↓
[Write to data.json]
    ↓
[
  {
    "data": "20260119",
    "result": "ACB,BID,VTP,..."
  },
  ...
]
```

## 🔧 API Endpoints

### `GET /api/stocks`
Trả về data ngày mới nhất + danh sách tất cả các ngày

### `GET /api/stocks/:date`
Trả về data ngày cụ thể (VD: `/api/stocks/20260119`)

### `GET /api/vn30`
Trả về danh sách VN30

### `POST /api/process-raw`
Process raw.json và update data.json (triggered by 📥 button)

## 💡 Tips

### Mỗi sáng:
1. Mở `data/raw.json`
2. Paste danh sách mã mới: `"symbols": "ACB,BID,..."`
3. Save
4. Click 📥 hoặc refresh page
5. Done! ✅

### Xem lịch sử:
- Dropdown "Chọn ngày" → chọn ngày cũ
- UI hiển thị data ngày đó

### Xử lý trùng lặp:
- Cùng ngày → auto merge + deduplicate
- Khác ngày → thêm entry mới

## 🎨 UI Features

- **Stats cards:** Ngày, Tổng mã, VN30, Khác
- **Date selector:** Dropdown với format "19/01/2026 (44 mã)"
- **VN30 icon:** ⭐ màu xanh bên cạnh symbol
- **TradingView button:** "Xem biểu đồ" với icon chart nến
- **Search:** Tìm kiếm nhanh theo mã
- **Responsive:** Mobile-friendly

## 🔮 Future Features

- [ ] Calendar view để duyệt lịch sử
- [ ] So sánh giữa các ngày
- [ ] Capture screenshot TradingView
- [ ] Export Excel/PDF
- [ ] Chart xu hướng xuất hiện của mã

---

*Built with Node.js, Express, Vue 3, Vuetify 3*
*Last updated: January 19, 2026*
